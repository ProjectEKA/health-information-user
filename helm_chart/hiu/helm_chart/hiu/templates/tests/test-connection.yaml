apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hiu.fullname" . }}-test-connection"
  labels:
{{ include "hiu.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "hiu.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
