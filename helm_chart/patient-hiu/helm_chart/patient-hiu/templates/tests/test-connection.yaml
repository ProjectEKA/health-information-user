apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "patient-hiu.fullname" . }}-test-connection"
  labels:
{{ include "patient-hiu.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "patient-hiu.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
